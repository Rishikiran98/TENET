# HIEROMEM

A semantic memory layer for AI agents. Store a memory (embed + persist) and
recall relevant memories (embed query → similarity search → rank → return),
scoped by namespace.

The design contract, architecture, and the layer roadmap live in
[CLAUDE.md](CLAUDE.md) — read that before changing anything.

> This is a placeholder. The full quickstart (run-with-zero-infra), the
> design rationale, and the layer roadmap are written in step 10 of the
> build sequence.
